# Welcome to Jekyll!
#
# This config file is meant for settings that affect your whole blog, values
# which you are expected to set up once and rarely edit after that. If you find
# yourself editing this file very often, consider using Jekyll's data files
# feature for the data you need to update frequently.
#
# For technical reasons, this file is *NOT* reloaded automatically when you use
# 'bundle exec jekyll serve'. If you change this file, please restart the server process.
#
# If you need help with YAML syntax, here are some quick references for you: 
# https://learn-the-web.algonquindesign.ca/topics/markdown-yaml-cheat-sheet/#yaml
# https://learnxinyminutes.com/docs/yaml/
#
# Site settings
# These are used to personalize your new site. If you look in the HTML files,
# you will see them accessed via {{ site.title }}, {{ site.email }}, and so on.
# You can create any custom variable you would like, and they will be accessible
# in the templates via {{ site.myvariable }}.

title: fieldWorks
email: info@field.works
description: >-
  A personal journal and portfolio at the intersection of GIS, LiDAR, prehistoric
  landscape, and the long human association with land. By Neil Jakeman.
baseurl: "" # the subpath of your site, e.g. /blog
url: "https://field.works"

author:
  name: Neil Jakeman
  email: info@field.works

google_site_verification: JPQeTPB5ieDk0Qz8sr2lukFHjLHcv8MuNPfR5tbLjjk
# twitter_username: fieldworks
github_username:  fieldWorks-3D
sketchfab_username: fieldworks
instagram_username: __fieldworks
vimeo_username: fieldworks3D



logo: /henge-bw-logo.png

# Build settings
# theme: minima
remote_theme: "mmistakes/jekyll-theme-basically-basic"
plugins:
  - jekyll-feed
  - jekyll-seo-tag
  - jekyll-sitemap

# Exclude from processing.
# The following items will not be processed, by default.
# Any item listed under the `exclude:` key here will be automatically added to
# the internal "default list".
#
# Excluded items can be processed by explicitly listing the directories or
# their entries' file path in the `include:` list.
#
# exclude:
#   - .sass-cache/
#   - .jekyll-cache/
#   - gemfiles/
#   - Gemfile
#   - Gemfile.lock
#   - node_modules/
#   - vendor/bundle/
#   - vendor/cache/
#   - vendor/gems/
#   - vendor/ruby/

# page behavior
defaults:
  -
    scope:
      type: posts
      path: _posts
    values:
      is_post: true
      layout: post
      permalink: /blog/:categories/:title/
  -
    scope:
      type: pages
      path: _pages
    values:
      is_post: false
      layout: default


include: [_pages,_services]

collections:
    services:
        output: true
        permalink: /:collection/:path/
        order:
            modelling.md
            scanning.md
            development.md
